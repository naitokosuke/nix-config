{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp dev",
    "build": "tsc && vp build",
    "preview": "vp preview",
    "deploy": "pnpm build && void deploy --dir dist/client --spa --skip-build"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "void": "^0.8.1"
  },
  "packageManager": "pnpm@11.2.2"
}
