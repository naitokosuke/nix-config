{
  system.defaults.CustomUserPreferences = {
    "com.apple.symbolichotkeys" = {
      AppleSymbolicHotKeys = {
        # Disable "Select the previous input source" (Ctrl+Space)
        "60" = {
          enabled = false;
        };
        # Disable "Select the next source in the Input Menu" (Ctrl+Option+Space)
        "61" = {
          enabled = false;
        };
      };
    };
  };
}
